/**
 * LMSetjen DPD RI - Application Version
 * Single source of truth for frontend versioning.
 * Keep this in sync with the root VERSION file.
 */

export const APP_VERSION = "1.0.1";
export const APP_NAME = "LMSetjen DPD RI";

/** Formatted display string, e.g. "LMSetjen DPD RI v1.8.0" */
export const APP_VERSION_LABEL = `${APP_NAME} v${APP_VERSION}`;
